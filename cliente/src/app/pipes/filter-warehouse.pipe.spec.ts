import { FilterWarehousePipe } from './filter-warehouse.pipe';

describe('FilterWarehousePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWarehousePipe();
    expect(pipe).toBeTruthy();
  });
});
